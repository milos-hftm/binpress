# Project-local Codex defaults for the BinPress static website.
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = true
